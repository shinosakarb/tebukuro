#!/usr/bin/env bash

case ${1} in
  start|s)
    docker-compose up
  ;;

  setup)
    docker-compose build
    docker-compose run --rm backend rails rake:db:setup
  ;;

  *)
    docker-compose exec spring $@
  ;;
esac
